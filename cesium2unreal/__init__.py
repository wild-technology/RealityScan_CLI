"""Place georeferenced Cesium ion tilesets into Unreal Engine levels.

`ion_locate` talks only to the Cesium ion REST API and runs anywhere.
`populate` drives the Unreal editor and must be imported with the editor's
own Python.
"""

__version__ = "0.1.0"
