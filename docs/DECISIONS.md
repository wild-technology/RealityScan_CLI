# Owner decisions - the open and settled record

One row per decision the code cannot make. An OPEN row is a stop: no agent
flips it. Numbering continues from `docs/history/AGENT_NATIVE_ROADMAP.md`.

| # | Decision | Status | Where it bites |
|---|---|---|---|
| D1 | Legacy identity-capture/default decision versus the new native calibration import lane | **LEGACY DECISION OPEN; deployment lane distinguished 2026-09-11.** Fresh deployment alignment uses native calibration-only XMP followed by CSV pose/accuracy import with regrouping disabled and a pre-alignment prior census. Scoped v02 native-XMP/CSV readback supports measured delivery fields, not physical calibration or blanket production acceptance; current serializer/census and remaining live proofs retain their own gates. This does not settle the historical legacy identity-capture default, the delegated CLI grouping question, or authorize changing RS_LEGACY_XMP_IDENTITY. See the dated legacy record below and current evidence ledger. | modules/camera_registry.py; AlignZone.bat; RS_LEGACY_XMP_IDENTITY; docs/EVIDENCE_LEDGER.json |
| D2 | Base branch for the 2026-09 reconciliation | SETTLED 2026-09-03: `main` | - |
| D3 | Current Hercules owner-selected mount/accuracy profile | **CURRENT OWNER POLICY 2026-09-11; earlier Zeuss 25/45 superseded.** Zeuss mount pitch 40 degrees down and pitch accuracy 40 degrees. Legacy upper/mid/lower mounts 70/20/10 degrees down, each pitch accuracy 10 degrees. All yaw/roll accuracies 10 degrees, orientation weight locked at 2; position accuracies 5/5/1 m and weight 10. Bake the general Hercules profile with project-effective values recorded; preserve other era-specific geometry. These are owner-selected starts, not empirical calibration or an optimality claim. | modules/cameras.json; modules/camera_registry.py; docs/VERIFICATION_STATUS.md |
| D4 | Delete or archive the manual UI and campaign drivers? | SETTLED 2026-09-05 (owner): **archive, keep functional** - `archive/wildscan_tui/` runs; drivers and probes archived as citation targets | `archive/` |
| D5 | `rs_settings.json` remembered answers | SETTLED: machine constants only (`realityscan.*`); science and paths come from the charter; `RS_NO_SETTINGS_INHERITANCE` is on for every chartered child | `module_base/settings_store.py` |
| D6 | Retire the old checkout under `coyotethings\tools` and bring its five staging scripts (`stage_wca_stills.py`, `convert_dng.py`, `crop_upper_1to1.py`, ...) into `modules/staging/` | **OPEN** - not done; the scripts are not in this repo. 2026-09-05: not on the Honeybadger box either (searched `C:\Users\jonat`, `D:`, `E:`, `F:`) - they exist only on the NA165 box under `C:\Users\produ\coyotethings\tools` | roadmap sec.1.6 |
| D7 | Chain stages run as ONE `main.py` invocation (in-process hand-off between Batch Directory and Alignment) - `rs run`/`launch` preserve this; per-stage launching would change data handling | SETTLED 2026-09-05: preserved | `modules/run_plan.build_commands` |
| D8 | From an agent harness (`CLAUDECODE` set) RealityScan stages may not run in the foreground; `rs launch` + `schtasks` is the path, `--foreground` is the owner's override | SETTLED 2026-09-05 (mandate 6 made mechanical) | `rs.py` |
| D9 | Promote `stage_features` out of the ON2026 campaign driver into `modules/feature_merge.py` | SETTLED 2026-09-06 (owner: do now) - `feature_merge.plan_feature_stage` + `FeatureStageAbort`; the driver is a wrapper under `archive/campaign_drivers/`; the three stage-F tests run the module | `modules/feature_merge.py` |
| D10 | Per-stage `<stage>_report.json` for extract/georeference/preprocess/export (roadmap Phase 2 step 3) | SETTLED 2026-09-06 (owner: "why is this necessary to census textures?" - it is not): the export census reads the export TREE (`modules/texture_census.py`: pages, JPEG, <= 4096, `map_Kd`) and BLOCKS on an untextured deliverable; no report file. Reports for extract/georeference/preprocess remain unneeded while `RUN_STATE.json` carries their exit codes | `modules/workspace_census.py`, `modules/export_deliverables.py` |
| D11 | The three legacy Cesium assets still sit at the sea surface (`2017323`, `2335997`, `2336618`); ion cannot reposition after tiling | **OPEN** - owner decision to re-publish from source | `publish_cesium.py` |
| D12 | Simplification strategy and the blind `-selectModel` + `-deleteSelectedModel` pattern (FINDINGS 2026-09-03, rs-reference 12 F-102) | SETTLED 2026-09-06 (owner): **simplify 75 % per pass until at or under 10,000,000 triangles (`RS_TARGET_TRIS`); small models need none** - `GenerateModel.bat` [7/8] and `ModelToFinal.bat` loop on the measured count (`Simplify75per_Params.xml`, model report via `model_report.py`); every select is proven and every delete verified in all three model/export workflows; an untextured deliverable aborts before the save. `run_decimate.py` keeps the 500k web tier. Live verification: `testing/NA173_TEST_PLAN.md` C2/C9 | `GenerateModel.bat`, `ModelToFinal.bat`, `ExportDeliverables.bat`, `modules/realityscan_interface/model_report.py` |
| D13 | Texture policy: `ModelToFinal.bat` paired an unwrap with the texture preset only for `4x8k`; every other preset fell through to a 1 × 16K unwrap (FINDINGS 2026-09-03, rs-reference 10 A4) | SETTLED 2026-09-05 (owner): **AdaptiveTexelSize at a 4096 cap in every workflow, never 16K, never a forced 4 × 8K unwrap, JPG textures on export** - `MaxTexturesCount` presets retired to `archive/metadata_retired/`, `:try_unwrap` falls back to 4 × 4096, `preflight` blocks regressions (`testing/test_texture_policy.py`) | `GenerateModel.bat`, `ModelToFinal.bat`, `RS_CLI/Metadata/`, `finish_model.py`, `modules/preflight.py` |
| D14 | Reconciliation cadence: a FINDINGS entry stating RealityScan behaviour is added to the matching `docs/rs-reference/` file in the same session (Addenda section) | SETTLED 2026-09-05 (this pass reconciled everything through 2026-09-03) | `.claude/skills/handoff` |
| D15 | Retire FINDINGS as current authority after the evidence audit | **OWNER DIRECTION SET 2026-09-11; evidence-led retirement PENDING.** Latest goal is full deprecation after audit, superseding the earlier keep-live policy. Reconcile atomic claims into maintained references and the evidence ledger, retain contradictions and unperformed probes, and preserve frozen historical provenance. Only evidence-supported scoped claims may be treated as verified. Routing/workflow migration and final retirement are not complete merely because documentation exists; main owns that migration. D14 describes historical reconciliation cadence, not a requirement to keep FINDINGS live indefinitely. | docs/EVIDENCE_LEDGER.json; docs/VERIFICATION_STATUS.md; docs/rs-reference/; main-owned routing migration |

## Dated prior decisions retained as provenance

The following original rows are preserved verbatim. Their status language applies
to the dates recorded, not today's deployment authority. D3 and D15 were
superseded by owner direction on 2026-09-11. D1's 2026-09-06 evidence remains a
historical scope of an OPEN legacy decision; the new native import lane does not
close it. "Known-good" and other historical labels do not extend beyond their
original evidence.

| # | Historical decision | Status at the recorded date | Historical scope |
|---|---|---|---|
| D1 | Do CLI prior groups (`-setPriorCalibrationGroup`/`-setPriorLensGroup`) take effect from the delegated CLI? main measured them silently non-functional (FINDINGS 2026-08-08); the sidecars line ran H2080/H2063 with them and never measured. | **OPEN, narrowed 2026-09-06** - the CSV lane (`identity_capture: csv`) ran F2 end to end: batch, two zones, merge, ZERO `*.xmp` beside any image (the merge peel census writes ordinal XMPs into its own attempt folder only), per-component `identity/*.csv` with the per-camera readback. Arm (i) of C6 measured: prior groups alone leave every camera its own focal (78 distinct of 78; FINDINGS `[RECON] 2026-09-06`). Still to run: arm (ii) XMP sidecars (known-good) and arm (iii) neither. Owner choice pending on the result; the scale oracle reads the identity CSVs since 2026-09-06 (FINDINGS `[HARNESS] 2026-09-06`, scale oracle entry), so `csv` no longer lacks a scale source | `RS_LEGACY_XMP_IDENTITY` default in `AlignZone.bat` and `realityscan_interface.py`; hard rule 0 |
| D3 | Adopt `85c556a` (Zeuss 25/45, orientation hardness 2.0)? | SETTLED 2026-09-05 (owner: yes) - re-applied as `677aa6c` on the reconciled tree; still un-A/B'd. The NA173 test dataset's supplied log was generated with the OLD mount (30/15 baked into its pitch columns) and carries no raw nav, so only the hardness half (2.0 vs 10.0) is testable as-is (`testing/NA173_TEST_PLAN.md` C4); the mount half needs a derived log (C5) | `modules/cameras.json`, `georeference_images.MOUNTS`, `AlignmentParams.xml` |
| D15 | Keep `FINDINGS.md` at all? | SETTLED 2026-09-06 (owner: agreed) - kept, guarded: entries before 2026-09-05 frozen in `docs/history/FINDINGS_2026-07_to_2026-09-03.md` (reconciled into `docs/rs-reference/` on 2026-09-05), `FINDINGS.md` is the header + the [RECON] D1 entry + the live tail; nothing prints it; the D14 same-session reconciliation rule stands | `CLAUDE.md` Findings section, `.claude/skills/handoff` |

## Recording a decision

Add a row here the moment the owner decides; cite the FINDINGS entry or the
chat quote in HANDOFF. Update an OPEN row only from explicit owner direction, with its date and scope; never infer resolution from a different lane or a passing test.
